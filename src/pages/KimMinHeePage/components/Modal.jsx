export default function Modal({ title, contents }) {
  return (
    <div
      className={`bg-white rounded-xl mx-auto mt-20 shadow-xl border border-black border-opacity-10`}
      style={{ width: `260px`, height: `280px` }}
    ></div>
  );
}
